# Copy this file to `.env` for local development (`.env` is gitignored).
# In production, set these in the Vercel project's Environment Variables.

# ── Supabase: stormsync-vip (ref sofrhcdjkjfphibysmxc) ───────────────────────
# These are PUBLISHABLE values — safe to expose in the browser. Database access is
# governed by Row Level Security, so the publishable key alone cannot read
# protected data.
VITE_SUPABASE_URL=https://sofrhcdjkjfphibysmxc.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_42Ptk6x18AyA4JYF2kCWIQ_Bh78q2Zt
