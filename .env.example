# Copy this file to `.env` for local development (`.env` is gitignored).
# In production, set these in the Vercel project's Environment Variables.

# ── Supabase: stormsync-vip (ref djonpetxdjuwcbgftqmt) ───────────────────────────
# These are PUBLISHABLE values — safe to expose in the browser. Database access is
# governed by Row Level Security, so the anon key alone cannot read protected data.
VITE_SUPABASE_URL=https://djonpetxdjuwcbgftqmt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_mamPPx1S8XKZkVy6LDGM-w_F50p9pCl
