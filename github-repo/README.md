# StormSync v5

## Your admin login
Email: JayMyers@StormSync.Media
PIN: 1337

## Deploy to Vercel
Framework: Vite | Build command: npm run build | Output directory: dist

## Run locally (requires Node >=20)
npm install && npm run dev  →  open http://localhost:5173

## Notes
- The 4 AI module pages need the backend server to be running separately.
  All other weather pages use free public APIs and work without it.
- This uses localStorage for auth (per-device). Supabase migration is Phase 2.
